# Ghost Rejector - Быстрая проверка работы

## ✅ Успех выглядит так:

### 1. На CNN.com баннера НЕТ
- Откройте https://edition.cnn.com/
- Баннер cookie НЕ появляется (или мелькнул и исчез)
- ✅ Это значит расширение работает!

### 2. В консоли есть логи
```
F12 → Console → видите:
✓ Ghost Rejector: Content script запущен на https://edition.cnn.com/
✓ Ghost Rejector: Загружено сигнатур: 2
✓ Ghost Rejector: Превентивная инъекция cookies...
✓ Ghost Rejector: Обнаружена CMP "OneTrust"
✓ Ghost Rejector: Cookie "OptanonConsent" установлен
✓ Ghost Rejector: Скрываем 1 элементов по селектору "#onetrust-banner-sdk"
```

### 3. Cookies установлены
Выполните в консоли:
```javascript
document.cookie.split(';').filter(c => c.includes('Consent'))
```

**Хороший результат (✅ работает):**
```javascript
Array(3) [
  " OptanonConsent=isGpcEnabled=0&datestamp=Mon+Dec+29+2025...",
  " CookieConsent={stamp:'ghost-rejector'...}",
  " OptanonConsent=isGpcEnabled=0..."
]
```
↑ Видите массив с элементами = cookies установлены

**Плохой результат (❌ НЕ работает):**
```javascript
Array(0) []
```
↑ Пустой массив = cookies НЕ установлены

### 4. Трекеры блокируются
```
F12 → Network → обновить страницу →
  ищите строки со статусом:
  ❌ net::ERR_BLOCKED_BY_CLIENT
```

Примеры заблокированных:
- `https://cdn.ml314.com/taglw.js` ❌ ERR_BLOCKED_BY_CLIENT
- `https://sb.scorecardresearch.com/...` ❌ ERR_BLOCKED_BY_CLIENT
- `https://secure.quantserve.com/...` ❌ ERR_BLOCKED_BY_CLIENT

## 🔍 Как проверить тип CMP на сайте

В консоли выполните:
```javascript
window.OneTrust      // Для OneTrust
window.Cookiebot     // Для Cookiebot
```

**Интерпретация результата:**

```javascript
// ✅ OneTrust ЕСТЬ на сайте:
window.OneTrust
// → {init: ƒ, IsAlertBoxClosed: ƒ, ...}  (вернулся объект)

// ❌ OneTrust НЕТ на сайте:
window.OneTrust
// → undefined

// Пример на CNN.com:
window.OneTrust      // → объект ✅ (значит OneTrust используется)
window.Cookiebot     // → undefined ❌ (Cookiebot не используется)
```

## 📊 Интерпретация результатов проверки cookies

### Команда для проверки:
```javascript
document.cookie.split(';').filter(c =>
  c.includes('Consent') || c.includes('Cookie') || c.includes('Privacy')
)
```

### Возможные результаты:

**1. Расширение работает идеально:**
```javascript
Array(2) [
  " OptanonConsent=isGpcEnabled=0&datestamp=Mon+Dec+29+2025+12:05:23+GMT+0300+(MSK)&version=202501.1.0&browserGpcFlag=0&isIABGlobal=false&hosts=&consentId=ghost-rejector-1766999123&interactionCount=1&landingPath=NotLandingPage&groups=1:1,2:0,3:0,4:0",
  " CookieConsent={stamp:'ghost-rejector',necessary:true,preferences:false,statistics:false,marketing:false,method:'explicit',ver:1,utc:1766999123675,region:'eu'}"
]
```
↑ Оба cookie установлены
↑ Обратите внимание на `groups=1:1,2:0,3:0,4:0` - это отказ от трекинга!

**2. Расширение установило только OneTrust:**
```javascript
Array(1) [
  " OptanonConsent=isGpcEnabled=0&..."
]
```
↑ Это нормально если сайт использует только OneTrust (как на CNN.com)

**3. Расширение НЕ работает:**
```javascript
Array(0) []
```
↑ Пустой массив = cookies НЕ установлены
↑ Проверьте что расширение включено в chrome://extensions/

**4. На сайте уже есть реальный consent cookie:**
```javascript
Array(1) [
  " OptanonConsent=groups=1:1,2:1,3:1,4:1..."
]
```
↑ Обратите внимание: `2:1,3:1,4:1` (все включено)
↑ Это означает что вы ранее дали согласие на сайте
↑ Решение: очистите cookies (F12 → Application → Clear storage)

## 🎯 Что означают groups в OptanonConsent

```
groups=1:1,2:0,3:0,4:0
       │  │  │  │  │
       │  │  │  │  └─ 4:0 = Targeting/Advertising OFF
       │  │  │  └──── 3:0 = Functional OFF
       │  │  └─────── 2:0 = Performance/Analytics OFF
       │  └────────── 1:1 = Strictly Necessary ON (обязательные)
       └───────────── Номер категории : Статус (1=ON, 0=OFF)
```

**Наш cookie (отказ от трекинга):**
`groups=1:1,2:0,3:0,4:0` = только необходимые включены ✅

**Полное согласие (плохо):**
`groups=1:1,2:1,3:1,4:1` = все включено, трекинг активен ❌

## 🛠️ Быстрое исправление проблем

### Баннер все еще видно?

**1. Обновите расширение:**
```
chrome://extensions/ → Ghost Rejector → кнопка Refresh (⟳)
```

**2. Очистите данные сайта:**
```
F12 → Application → Clear storage → Clear site data → Refresh
```

**3. Жесткая перезагрузка:**
```
Ctrl+Shift+R (Windows/Linux)
Cmd+Shift+R (Mac)
```

### Нет логов в консоли?

**Проверьте что расширение активно:**
```
chrome://extensions/ →
  Ghost Rejector →
  переключатель должен быть ON (синий)
```

### Cookies не устанавливаются?

**Возможная причина: CSP блокирует**
```javascript
// Проверьте можно ли вообще ставить cookies:
document.cookie = "test=123; path=/; SameSite=Lax"
document.cookie  // если "test" нет в результате = CSP блокирует
```

## 📝 Когда сообщать об ошибке

Сообщите об ошибке если:
- ❌ Баннер видно после всех шагов выше
- ❌ В консоли ошибка "Ghost Rejector: ..."
- ❌ Cookies пустой массив `[]`
- ❌ Сайт сломался/не работает

**Что приложить к отчету:**
1. URL сайта
2. Скриншот консоли с логами
3. Результат команды проверки cookies (скопируйте массив)
4. Результат `window.OneTrust` и `window.Cookiebot`
