# Ghost Rejector

Расширение для Chrome которое автоматически блокирует трекеры и убирает надоедливые cookie баннеры.

## Что делает

1. **Блокирует ~5000 трекеров** из списка EasyPrivacy (Google Analytics, Facebook Pixel, DoubleClick и т.д.)
2. **Автоматически отклоняет cookie баннеры** OneTrust и Cookiebot
3. **Добавляет GPC заголовок** (Global Privacy Control) ко всем запросам

## Быстрый старт

### Шаг 1: Генерация правил

```bash
cd builder
go run .
```

Это скачает EasyPrivacy список и создаст файлы:
- `extension/rules/net_rules.json` - правила блокировки трекеров
- `extension/rules/signatures.json` - сигнатуры CMP систем

### Шаг 2: Установка расширения

1. Откройте Chrome
2. Перейдите на `chrome://extensions/`
3. Включите "Режим разработчика" (Developer mode) в правом верхнем углу
4. Нажмите "Загрузить распакованное расширение" (Load unpacked)
5. Выберите папку `extension/`

Готово! Расширение активно.

## Проверка работы

### Тест блокировки трекеров

1. Откройте `cnn.com`
2. Нажмите F12 → вкладка Network
3. Обновите страницу
4. Ищите запросы со статусом `(blocked:other)` - это заблокированные трекеры

### Тест OneTrust

1. Откройте `cnn.com` или `microsoft.com`
2. Баннер cookie **не должен появиться** или быстро исчезнет
3. F12 → Console → должны быть логи `Ghost Rejector: Обнаружена CMP "OneTrust"`
4. F12 → Application → Cookies → должен быть cookie `OptanonConsent`

### Тест Cookiebot

1. Откройте `cookiebot.com/en/`
2. Баннер **не должен появиться**
3. Аналогично проверьте консоль и cookies

## 🔍 Диагностика проблем

### Если баннер не исчезает

**Шаг 1: Проверьте что расширение загружено**
1. Откройте `chrome://extensions/`
2. Найдите "Ghost Rejector"
3. Проверьте что переключатель включен
4. Если есть ошибки - нажмите "Errors" и скопируйте их

**Шаг 2: Откройте консоль на проблемной странице**
1. Откройте сайт с баннером (например, `cnn.com`)
2. Нажмите F12 → вкладка Console
3. Обновите страницу (Ctrl+R или Cmd+R)
4. Ищите логи начинающиеся с `Ghost Rejector:`

**Что должно быть в консоли:**
```
Ghost Rejector: Content script запущен на https://edition.cnn.com/
Ghost Rejector: Загружено сигнатур: 2
Ghost Rejector: Превентивная инъекция cookies...
Ghost Rejector: Обнаружена CMP "OneTrust"
Ghost Rejector: Cookie "OptanonConsent" установлен
Ghost Rejector: Скрываем N элементов по селектору "#onetrust-banner-sdk"
Ghost Rejector: navigator.globalPrivacyControl установлен
```

**Шаг 3: Проверьте cookies**
1. F12 → Application → слева в дереве найдите "Cookies"
2. Раскройте "Cookies" → выберите `https://edition.cnn.com` (или другой домен)
3. В таблице справа найдите cookies:
   - `OptanonConsent` (для OneTrust) - если есть, значит ✅ установлен
   - `CookieConsent` (для Cookiebot) - если есть, значит ✅ установлен
4. Кликните на cookie чтобы увидеть полное значение
5. Для OneTrust проверьте что в Value есть `groups=1:1,2:0,3:0,4:0` (это отказ от трекинга)

**Альтернативный способ через Console:**
```javascript
// Показать все cookies связанные с consent
document.cookie.split(';').filter(c =>
  c.includes('Consent') ||
  c.includes('Cookie') ||
  c.includes('Privacy')
)
```

**Что означает результат:**
```javascript
// Если вернулся массив с элементами - cookies установлены ✅
// Пример хорошего результата:
[
  "OptanonConsent=isGpcEnabled=0&datestamp=...",
  "CookieConsent={stamp:'ghost-rejector',...}"
]

// Если пустой массив [] - cookies НЕ установлены ❌
```

**Шаг 4: Проверьте DOM**
1. F12 → Elements
2. Ctrl+F (Cmd+F) → поиск по `onetrust` или `cookiebot`
3. Если находятся элементы - проверьте их стили (должны быть `display: none`)

**Шаг 5: Проверьте Network для блокировок**
1. F12 → Network
2. Обновите страницу
3. Ищите запросы к `google-analytics.com`, `doubleclick.net`, etc.
4. Статус должен быть `(blocked:other)` или `(blocked: extension)`

### Сбор информации для отчета об ошибке

Если расширение не работает на конкретном сайте, соберите следующую информацию:

**1. URL сайта:**
```
https://example.com
```

**2. Логи из консоли:**
```
F12 → Console → скопируйте все строки с "Ghost Rejector"
```

**3. Тип CMP (если известен):**
```javascript
// F12 → Console → введите эти команды:
window.OneTrust
window.Cookiebot

// Интерпретация:
// Если вернулся объект {init: ƒ, ...} - эта CMP ЕСТЬ на сайте
// Если вернулся undefined - этой CMP НЕТ на сайте
// Пример: на CNN.com window.OneTrust вернет объект = OneTrust используется
```

**4. Селектор баннера:**
```
F12 → Elements → найдите баннер →
Правой кнопкой → Copy → Copy selector
// Вставьте скопированный селектор
```

**5. Cookies на сайте:**
```javascript
// Способ 1 (рекомендуется) - через Console:
// F12 → Console → выполните команду:
document.cookie.split(';').filter(c =>
  c.includes('Consent') || c.includes('Cookie') || c.includes('Privacy')
)

// Скопируйте результат массива
// Пример хорошего результата:
// Array(3) [ " OptanonConsent=...", " CookieConsent=...", ... ]

// Способ 2 - через Application:
// F12 → Application → слева "Cookies" → выбрать домен →
// Скопируйте имена cookies из столбца "Name"
```

**6. Ошибки расширения:**
```
chrome://extensions/ →
Найдите Ghost Rejector →
Если есть кнопка "Errors" - нажмите и скопируйте текст
```

### Частые проблемы

**Проблема: "Не удалось загрузить сигнатуры"**
- Решение: Убедитесь что файл `extension/rules/signatures.json` существует
- Запустите `cd builder && go run .` для генерации

**Проблема: Баннер появляется на секунду и исчезает**
- Это нормально - расширение работает после загрузки страницы
- Можно улучшить добавив больше селекторов для конкретной CMP

**Проблема: Баннер не исчезает вообще**
- Возможно это новая CMP система которую мы не поддерживаем
- Соберите информацию по инструкции выше

**Проблема: Трекеры не блокируются**
- Проверьте что файл `extension/rules/net_rules.json` существует и не пустой
- Размер файла должен быть ~1.5 МБ

### Тестирование изменений

После внесения изменений в код:

**1. Обновите расширение:**
```bash
# Если изменили Go код:
cd builder && go run .

# В Chrome:
chrome://extensions/ →
Найдите Ghost Rejector →
Нажмите кнопку "Обновить" (иконка refresh)
```

**2. Очистите cookies и кэш:**
```
F12 → Application → Clear storage → Clear site data
```

**3. Перезагрузите страницу:**
```
Ctrl+Shift+R (Cmd+Shift+R) - жесткая перезагрузка
```

## Как это работает

### Архитектура

```
builder/                    # Go приложение для генерации правил
├── main.go                 # Точка входа
├── easyprivacy.go          # Парсинг EasyPrivacy
├── gpc.go                  # GPC заголовок
├── signatures.go           # Сигнатуры OneTrust/Cookiebot
├── types.go                # Типы данных
└── utils.go                # Утилиты

extension/                  # Chrome расширение
├── manifest.json           # Манифест MV3
├── background.js           # Service worker
├── content.js              # Content script
├── popup.html              # UI
├── popup.js                # UI логика
└── rules/
    ├── net_rules.json      # Генерируется builder
    └── signatures.json     # Генерируется builder
```

### Принцип работы

**Блокировка трекеров:**
- Chrome DeclarativeNetRequest API блокирует запросы по ~5000 правилам
- Правила генерируются из EasyPrivacy списка
- Блокируются домены вида `||google-analytics.com^`

**Обход cookie баннеров:**
1. Content script выполняется на `document_start` (до загрузки страницы)
2. Определяет CMP по детекторам (window объекты или DOM элементы)
3. Инъецирует cookie с "отказом от всего кроме необходимых"
4. Скрывает баннеры через CSS (`display: none`)
5. Повторная проверка через 1 секунду для асинхронных CMP

**OneTrust cookie формат:**
```
OptanonConsent=isGpcEnabled=0&datestamp=...&groups=1:1,2:0,3:0,4:0
```
- `1:1` - Strictly Necessary (разрешены)
- `2:0` - Performance (запрещены)
- `3:0` - Functional (запрещены)
- `4:0` - Targeting (запрещены)

**Cookiebot cookie формат:**
```
CookieConsent={necessary:true,preferences:false,statistics:false,marketing:false}
```

## Поддерживаемые CMP системы

- **OneTrust** - используется на ~40% сайтов топ-1000 (CNN, Microsoft, Oracle)
- **Cookiebot** - популярен в Европе на средних сайтах

## Ограничения

- Работает на 60-70% сайтов с этими CMP (форматы cookie могут отличаться)
- Некоторые сайты могут восстановить баннер через JS
- CSP политики могут заблокировать инъекцию на отдельных сайтах

## Обновление правил

Чтобы обновить список трекеров:

```bash
cd builder
go run .
```

Затем перезагрузите расширение в `chrome://extensions/`

## Структура кода

### Go Builder

- `easyprivacy.go` - скачивание и парсинг EasyPrivacy в DNR правила
- `gpc.go` - создание правила для GPC заголовка
- `signatures.go` - генерация сигнатур OneTrust и Cookiebot с актуальными cookie
- `types.go` - структуры данных (DNRRule, CMPSignature, Cookie)
- `utils.go` - сохранение JSON

### Extension

- `content.js` - основная логика детекции CMP и инъекции cookies
- `background.js` - service worker (пока только для badge)
- `popup.html/js` - простой UI показывающий статус

## Дальнейшие улучшения

- Добавить больше CMP: Didomi, Quantcast Choice, TrustArc
- Whitelist для сайтов где не нужна блокировка
- Статистика блокировок в popup
- Auto-update правил из GitHub

## Лицензия

MIT
